# ПЛОХОЙ Dockerfile - тут много ошибок!

# Ошибка 1: Берем latest - неизвестно что придет
FROM python:latest

# Ошибка 2: Работаем от root (по умолчанию)
WORKDIR /app

# Ошибка 3: Копируем всё сразу (включая мусор)
COPY . /app

# Ошибка 4: Не чистим кэш - образ раздувается
RUN pip install -r requirements.txt

# Ошибка 5: Нет HEALTHCHECK - не узнаем, что приложение умерло
# Ошибка 6: Нет обработки сигналов - при остановке может убиться не gracefully
CMD ["python", "app.py"]
